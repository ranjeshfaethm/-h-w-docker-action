name: 'h-w-docker-action'
description: 'run hello world on docker'

inputs:
  who-to-greet:
    description: 'who-to-greet'
    default: 'Shazaam!'
runs:
  using: 'docker'
  image: 'docker/Dockerfile'
